const express = require('express');
const app = express();

//require('express')().listen(4000, () => console.log('Now listening for requests on port 4000'));

app.listen(4000, () => console.log('Now listening for requests on port 4000'));